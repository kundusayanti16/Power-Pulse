{
  "name": "power-pulse",
  "version": "1.0.0",
  "description": "Smart Electricity Consumer Complaint & Feedback Portal",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm run dev",
    "frontend": "npx serve frontend",
    "seed": "cd backend && node seed-admin.js"
  }
}
